{
  "name": "routes-generator",
  "version": "1.0.0",
  "description": "REST like URL routes generator for frontend (for react-router etc)",
  "main": "routes-generator.js",
  "typings": "routes-generator.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "narazaka",
  "license": "MIT",
  "dependencies": {
    "tslint": "^5.7.0",
    "typedoc": "^0.9.0",
    "typescript": "^2.5.3"
  }
}
