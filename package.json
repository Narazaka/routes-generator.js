{
  "name": "routes-generator",
  "version": "0.1.4",
  "description": "REST like URL routes generator for frontend (for react-router etc)",
  "main": "./dist/routes-generator.js",
  "types": "./dist/routes-generator.d.ts",
  "files": [
    "dist"
  ],
  "keywords": [
    "routes",
    "path",
    "url",
    "react",
    "router"
  ],
  "scripts": {
    "prepublish": "npm run dist",
    "dist": "tsc",
    "test": "mocha",
    "lint": "tslint -p tsconfig.json --type-check src/routes-generator.ts",
    "doc": "typedoc --out doc --disableOutputCheck --excludeNotExported --excludeExternals src/routes-generator.ts"
  },
  "author": "narazaka",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^2.2.43",
    "@types/power-assert": "^1.4.29",
    "espower-typescript": "^8.1.1",
    "mocha": "^4.0.1",
    "power-assert": "^1.4.4",
    "tslint": "^5.7.0",
    "typedoc": "^0.9.0",
    "typescript": "^2.5.3"
  }
}
